{
  "name": "nook",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @nook/client",
    "build": "npm run build --workspace @nook/client",
    "lint": "npm run lint --workspace @nook/client",
    "preview": "npm run preview --workspace @nook/client"
  }
}
